{
  "name": "ronreed.com",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "video"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run develop --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "dev:video": "npm run dev --workspace=video",
    "render:video": "npm run render --workspace=video --"
  }
}
